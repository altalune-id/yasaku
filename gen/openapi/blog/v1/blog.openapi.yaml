openapi: 3.1.0
info:
  title: blog.v1
paths:
  /blog.v1.BlogService/CreatePost:
    post:
      tags:
        - blog.v1.BlogService
      summary: CreatePost
      operationId: blog.v1.BlogService.CreatePost
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/blog.v1.CreatePostRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/blog.v1.CreatePostResponse'
  /blog.v1.BlogService/DeletePost:
    post:
      tags:
        - blog.v1.BlogService
      summary: DeletePost
      operationId: blog.v1.BlogService.DeletePost
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/blog.v1.DeletePostRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/blog.v1.DeletePostResponse'
  /blog.v1.BlogService/GetPost:
    post:
      tags:
        - blog.v1.BlogService
      summary: GetPost
      operationId: blog.v1.BlogService.GetPost
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/blog.v1.GetPostRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/blog.v1.GetPostResponse'
  /blog.v1.BlogService/ListPosts:
    post:
      tags:
        - blog.v1.BlogService
      summary: ListPosts
      operationId: blog.v1.BlogService.ListPosts
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/blog.v1.ListPostsRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/blog.v1.ListPostsResponse'
  /blog.v1.BlogService/PublishPost:
    post:
      tags:
        - blog.v1.BlogService
      summary: PublishPost
      operationId: blog.v1.BlogService.PublishPost
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/blog.v1.PublishPostRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/blog.v1.PublishPostResponse'
  /blog.v1.BlogService/UnpublishPost:
    post:
      tags:
        - blog.v1.BlogService
      summary: UnpublishPost
      operationId: blog.v1.BlogService.UnpublishPost
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/blog.v1.UnpublishPostRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/blog.v1.UnpublishPostResponse'
  /blog.v1.BlogService/UpdatePost:
    post:
      tags:
        - blog.v1.BlogService
      summary: UpdatePost
      operationId: blog.v1.BlogService.UpdatePost
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/blog.v1.UpdatePostRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/blog.v1.UpdatePostResponse'
components:
  schemas:
    blog.v1.Category:
      type: object
      properties:
        id:
          type: string
          title: id
        name:
          type: string
          title: name
        slug:
          type: string
          title: slug
      title: Category
      additionalProperties: false
    blog.v1.CreatePostRequest:
      type: object
      properties:
        projectId:
          type: string
          title: project_id
        categoryId:
          type: string
          title: category_id
        title:
          type: string
          title: title
        slug:
          type: string
          title: slug
        bodyMarkdown:
          type: string
          title: body_markdown
        tagIds:
          type: array
          items:
            type: string
          title: tag_ids
      title: CreatePostRequest
      additionalProperties: false
    blog.v1.CreatePostResponse:
      type: object
      properties:
        post:
          title: post
          $ref: '#/components/schemas/blog.v1.Post'
      title: CreatePostResponse
      additionalProperties: false
    blog.v1.DeletePostRequest:
      type: object
      properties:
        postId:
          type: string
          title: post_id
      title: DeletePostRequest
      additionalProperties: false
    blog.v1.DeletePostResponse:
      type: object
      title: DeletePostResponse
      additionalProperties: false
    blog.v1.GetPostRequest:
      type: object
      properties:
        postId:
          type: string
          title: post_id
      title: GetPostRequest
      additionalProperties: false
    blog.v1.GetPostResponse:
      type: object
      properties:
        post:
          title: post
          $ref: '#/components/schemas/blog.v1.Post'
      title: GetPostResponse
      additionalProperties: false
    blog.v1.ListPostsRequest:
      type: object
      properties:
        projectId:
          type: string
          title: project_id
        status:
          type: string
          title: status
        categoryId:
          type: string
          title: category_id
      title: ListPostsRequest
      additionalProperties: false
    blog.v1.ListPostsResponse:
      type: object
      properties:
        posts:
          type: array
          items:
            $ref: '#/components/schemas/blog.v1.Post'
          title: posts
      title: ListPostsResponse
      additionalProperties: false
    blog.v1.Post:
      type: object
      properties:
        id:
          type: string
          title: id
        projectId:
          type: string
          title: project_id
        title:
          type: string
          title: title
        slug:
          type: string
          title: slug
        bodyMarkdown:
          type: string
          title: body_markdown
        bodyHtml:
          type: string
          title: body_html
        status:
          type: string
          title: status
        category:
          title: category
          $ref: '#/components/schemas/blog.v1.Category'
        tags:
          type: array
          items:
            $ref: '#/components/schemas/blog.v1.Tag'
          title: tags
        firstPublishedAt:
          title: first_published_at
          $ref: '#/components/schemas/google.protobuf.Timestamp'
        createdAt:
          title: created_at
          $ref: '#/components/schemas/google.protobuf.Timestamp'
        updatedAt:
          title: updated_at
          $ref: '#/components/schemas/google.protobuf.Timestamp'
      title: Post
      additionalProperties: false
    blog.v1.PublishPostRequest:
      type: object
      properties:
        postId:
          type: string
          title: post_id
      title: PublishPostRequest
      additionalProperties: false
    blog.v1.PublishPostResponse:
      type: object
      properties:
        post:
          title: post
          $ref: '#/components/schemas/blog.v1.Post'
      title: PublishPostResponse
      additionalProperties: false
    blog.v1.Tag:
      type: object
      properties:
        id:
          type: string
          title: id
        name:
          type: string
          title: name
        slug:
          type: string
          title: slug
      title: Tag
      additionalProperties: false
    blog.v1.UnpublishPostRequest:
      type: object
      properties:
        postId:
          type: string
          title: post_id
      title: UnpublishPostRequest
      additionalProperties: false
    blog.v1.UnpublishPostResponse:
      type: object
      properties:
        post:
          title: post
          $ref: '#/components/schemas/blog.v1.Post'
      title: UnpublishPostResponse
      additionalProperties: false
    blog.v1.UpdatePostRequest:
      type: object
      properties:
        postId:
          type: string
          title: post_id
        categoryId:
          type: string
          title: category_id
        title:
          type: string
          title: title
        slug:
          type: string
          title: slug
        bodyMarkdown:
          type: string
          title: body_markdown
        tagIds:
          type: array
          items:
            type: string
          title: tag_ids
      title: UpdatePostRequest
      additionalProperties: false
    blog.v1.UpdatePostResponse:
      type: object
      properties:
        post:
          title: post
          $ref: '#/components/schemas/blog.v1.Post'
      title: UpdatePostResponse
      additionalProperties: false
    connect-protocol-version:
      type: number
      title: Connect-Protocol-Version
      enum:
        - 1
      description: Define the version of the Connect protocol
      const: 1
    connect-timeout-header:
      type: number
      title: Connect-Timeout-Ms
      description: Define the timeout, in ms
    connect.error:
      type: object
      properties:
        code:
          type: string
          examples:
            - not_found
          enum:
            - canceled
            - unknown
            - invalid_argument
            - deadline_exceeded
            - not_found
            - already_exists
            - permission_denied
            - resource_exhausted
            - failed_precondition
            - aborted
            - out_of_range
            - unimplemented
            - internal
            - unavailable
            - data_loss
            - unauthenticated
          description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
        message:
          type: string
          description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
        details:
          type: array
          items:
            $ref: '#/components/schemas/connect.error_details.Any'
          description: A list of messages that carry the error details. There is no limit on the number of messages.
      title: Connect Error
      additionalProperties: true
      description: 'Error type returned by Connect: https://connectrpc.com/docs/go/errors/#http-representation'
    connect.error_details.Any:
      type: object
      properties:
        type:
          type: string
          description: 'A URL that acts as a globally unique identifier for the type of the serialized message. For example: `type.googleapis.com/google.rpc.ErrorInfo`. This is used to determine the schema of the data in the `value` field and is the discriminator for the `debug` field.'
        value:
          type: string
          format: binary
          description: The Protobuf message, serialized as bytes and base64-encoded. The specific message type is identified by the `type` field.
        debug:
          oneOf:
            - type: object
              title: Any
              additionalProperties: true
              description: Detailed error information.
          discriminator:
            propertyName: type
          title: Debug
          description: Deserialized error detail payload. The 'type' field indicates the schema. This field is for easier debugging and should not be relied upon for application logic.
      additionalProperties: true
      description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message, with an additional debug field for ConnectRPC error details.
    google.protobuf.Timestamp:
      type: string
      examples:
        - "2023-01-15T01:30:15.01Z"
        - "2024-12-25T12:00:00Z"
      format: date-time
      description: |-
        A Timestamp represents a point in time independent of any time zone or local
         calendar, encoded as a count of seconds and fractions of seconds at
         nanosecond resolution. The count is relative to an epoch at UTC midnight on
         January 1, 1970, in the proleptic Gregorian calendar which extends the
         Gregorian calendar backwards to year one.

         All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
         second table is needed for interpretation, using a [24-hour linear
         smear](https://developers.google.com/time/smear).

         The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
         restricting to that range, we ensure that we can convert to and from [RFC
         3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.

         # Examples

         Example 1: Compute Timestamp from POSIX `time()`.

             Timestamp timestamp;
             timestamp.set_seconds(time(NULL));
             timestamp.set_nanos(0);

         Example 2: Compute Timestamp from POSIX `gettimeofday()`.

             struct timeval tv;
             gettimeofday(&tv, NULL);

             Timestamp timestamp;
             timestamp.set_seconds(tv.tv_sec);
             timestamp.set_nanos(tv.tv_usec * 1000);

         Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.

             FILETIME ft;
             GetSystemTimeAsFileTime(&ft);
             UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;

             // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
             // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
             Timestamp timestamp;
             timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
             timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));

         Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.

             long millis = System.currentTimeMillis();

             Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
                 .setNanos((int) ((millis % 1000) * 1000000)).build();

         Example 5: Compute Timestamp from Java `Instant.now()`.

             Instant now = Instant.now();

             Timestamp timestamp =
                 Timestamp.newBuilder().setSeconds(now.getEpochSecond())
                     .setNanos(now.getNano()).build();

         Example 6: Compute Timestamp from current time in Python.

             timestamp = Timestamp()
             timestamp.GetCurrentTime()

         # JSON Mapping

         In JSON format, the Timestamp type is encoded as a string in the
         [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
         format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
         where {year} is always expressed using four digits while {month}, {day},
         {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
         seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
         are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
         is required. A proto3 JSON serializer should always use UTC (as indicated by
         "Z") when printing the Timestamp type and a proto3 JSON parser should be
         able to accept both UTC and other timezones (as indicated by an offset).

         For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
         01:30 UTC on January 15, 2017.

         In JavaScript, one can convert a Date object to this format using the
         standard
         [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
         method. In Python, a standard `datetime.datetime` object can be converted
         to this format using
         [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
         the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
         the Joda Time's [`ISODateTimeFormat.dateTime()`](
         http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
         ) to obtain a formatter capable of generating timestamps in this format.
security: []
tags:
  - name: blog.v1.BlogService
