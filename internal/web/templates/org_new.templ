package templates

import "altalune.id/yasaku/internal/web"

type OrgNewView struct {
	Slug  string
	Name  string
	Error string
	ErrorCode string
}

templ OrgNewPage(d web.LayoutData, v OrgNewView) {
	<section class="mx-auto max-w-xl">
		<header class="border-b border-border pb-4">
			<h1 class="text-2xl font-semibold text-foreground">{ d.Tr("nav.create_organization") }</h1>
			<p class="mt-1 text-sm text-muted-foreground">{ d.Tr("org_new.subtitle") }</p>
		</header>
		if v.Error != "" {
			@ErrorBanner(d, v.Error, v.ErrorCode)
		}
		<form method="post" action={ templ.SafeURL(d.Href("/orgs")) } class="mt-6 space-y-5" novalidate>
			<div>
				<label for="org-name" class="block text-sm font-medium text-foreground/90">{ d.Tr("common.name") }</label>
				<input id="org-name" name="name" type="text" required autofocus value={ v.Name } placeholder={ d.Tr("org_new.name_placeholder") } class="mt-1.5 block w-full rounded-md border border-border bg-card px-3 py-2 text-sm text-foreground placeholder:text-muted-foreground/70 shadow-sm focus:border-ring focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-1"/>
			</div>
			<div>
				<label for="org-slug" class="block text-sm font-medium text-foreground/90">{ d.Tr("common.slug") }</label>
				<input id="org-slug" name="slug" type="text" required value={ v.Slug } placeholder={ d.Tr("org_new.slug_placeholder") } pattern="[a-z0-9-]+" class="mt-1.5 block w-full rounded-md border border-border bg-card px-3 py-2 text-sm text-foreground placeholder:text-muted-foreground/70 shadow-sm focus:border-ring focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-1"/>
				<p class="mt-1.5 text-xs text-muted-foreground">{ d.Tr("common.slug_hint") }</p>
			</div>
			<div class="flex items-center gap-3 pt-2">
				<button type="submit" class="rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground shadow-sm hover:bg-primary/90">{ d.Tr("nav.create_organization") }</button>
				<a href={ templ.SafeURL(d.Href("/")) } class="text-sm text-muted-foreground hover:text-foreground">{ d.Tr("common.cancel") }</a>
			</div>
		</form>
	</section>
}

templ OrgNewLayout(d web.LayoutData, v OrgNewView) {
	@Layout(web.WithContent(d, OrgNewPage(d, v)))
}
