# hadolint config — https://github.com/hadolint/hadolint
ignored:
  - DL3008  # apk versions pinned by hash via digest — allow unpinned in golang:alpine
  - DL3018  # ditto for apk add
  - DL3006  # base images pinned by digest via ARG default; hadolint doesn't resolve ARG substitution
failure-threshold: warning
